#!/bin/bash
# usage:  htmlgen.sh "dir"

. bin/constants.sh

echo "xml2edn" >> logs/xml2edn.log;

#for d in `ls -d data`
#do
    # echo "$d ********************************************"
	#fs=`find $d -name *xml`
o#done

